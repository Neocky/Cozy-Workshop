


ItemEvents.tooltip(event => {
    //event.add("balancedflight:ascended_flight_ring", "An incredibly dense ring.", "Despite its weight it allows you to fly everywhere (Angel Ring).", "Allows both Creative and enhanced Elytra flight.")
    /*
    event.addAdvanced("balancedflight:ascended_flight_ring", (item, advanced, text) => {
        if (!event.isShift())
        {
            text.add(1, [
                Text.of("Hold [").darkGray(),
                Text.of("Shift").gray(),
                Text.of("] for summary").darkGray()
            ])
            text.add(2, [])
            text.add(3, [])
        }
        else if (event.isShift())
        {
            text.add(1, [
                Text.of("Allows both Creative and enhanced Elytra flight.")
            ])
            text.add(2, [
                Text.of("Slot: ").gold(),
                Text.of("Flight Ring").yellow()
            ])
        }
    })
    */
})
